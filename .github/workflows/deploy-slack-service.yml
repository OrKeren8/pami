name: Deploy Slack Service

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'slack_service/**'
      - '.github/workflows/deploy-slack-service.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: us-east-1

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and Push Image
        run: |
          cd slack_service

          IMAGE_TAG=${{ github.sha }}

          docker build -t pami-slack-service:$IMAGE_TAG .
          docker tag pami-slack-service:$IMAGE_TAG 909189231170.dkr.ecr.us-east-1.amazonaws.com/pami/slack-service:$IMAGE_TAG
          docker tag pami-slack-service:$IMAGE_TAG 909189231170.dkr.ecr.us-east-1.amazonaws.com/pami/slack-service:latest

          docker push 909189231170.dkr.ecr.us-east-1.amazonaws.com/pami/slack-service:$IMAGE_TAG
          docker push 909189231170.dkr.ecr.us-east-1.amazonaws.com/pami/slack-service:latest

      - name: Register Task Definition
        run: |
          cat > task-def.json << 'EOF'
          {
            "family": "pami-slack-service",
            "networkMode": "awsvpc",
            "requiresCompatibilities": ["FARGATE"],
            "cpu": "512",
            "memory": "1024",
            "executionRoleArn": "arn:aws:iam::909189231170:role/LabRole",
            "taskRoleArn": "arn:aws:iam::909189231170:role/LabRole",
            "containerDefinitions": [
              {
                "name": "slack-service",
                "image": "909189231170.dkr.ecr.us-east-1.amazonaws.com/pami/slack-service:latest",
                "portMappings": [
                  {
                    "containerPort": 8002,
                    "protocol": "tcp",
                    "appProtocol": "http"
                  }
                ],
                "essential": true,
                "environment": [
                  {
                    "name": "MONGODB_URL",
                    "value": "${{ secrets.MONGODB_URL }}"
                  },
                  {
                    "name": "SLACK_BOT_TOKEN",
                    "value": "${{ secrets.SLACK_BOT_TOKEN }}"
                  },
                  {
                    "name": "SLACK_SIGNING_SECRET",
                    "value": "${{ secrets.SLACK_SIGNING_SECRET }}"
                  }
                ],
                "logConfiguration": {
                  "logDriver": "awslogs",
                  "options": {
                    "awslogs-group": "/ecs/pami-slack-service",
                    "awslogs-create-group": "true",
                    "awslogs-region": "us-east-1",
                    "awslogs-stream-prefix": "ecs"
                  }
                }
              }
            ]
          }
          EOF

          aws ecs register-task-definition --cli-input-json file://task-def.json

      - name: Create or Update ECS Service
        run: |
          # Check if service exists
          if aws ecs describe-services --cluster pami-cluster --services pami-slack-service --region us-east-1 | grep -q "ACTIVE\|DRAINING"; then
            echo "Service exists, updating..."
            aws ecs update-service \
              --cluster pami-cluster \
              --service pami-slack-service \
              --force-new-deployment \
              --region us-east-1
          else
            echo "Service doesn't exist, creating..."
            aws ecs create-service \
              --cluster pami-cluster \
              --service-name pami-slack-service \
              --task-definition pami-slack-service \
              --desired-count 1 \
              --launch-type FARGATE \
              --network-configuration "awsvpcConfiguration={subnets=[subnet-047bd3bcf439f9667,subnet-0617c57f1370ef445],securityGroups=[sg-0a6ab09920fd49e29],assignPublicIp=ENABLED}" \
              --load-balancers "targetGroupArn=arn:aws:elasticloadbalancing:us-east-1:909189231170:targetgroup/slack-service-tg/07a05ce456b9b8bb,containerName=slack-service,containerPort=8002" \
              --region us-east-1
          fi

      - name: Wait for deployment to complete
        run: |
          aws ecs wait services-stable \
            --cluster pami-cluster \
            --services pami-slack-service \
            --region us-east-1

      - name: Get service status
        run: |
          echo "Deployment completed successfully!"
          aws ecs describe-services \
            --cluster pami-cluster \
            --services pami-slack-service \
            --region us-east-1 \
            --query 'services[0].taskDefinition' \
            --output text